# Milestone 4: Flow Builder & Persistence

## 4.1 Local Storage
- [ ] Create `useUserFlows` hook: CRUD operations on `localStorage`.
- [ ] Develop `FlowSchema` validation for user-created flows.

## 4.2 Builder UI (`src/features/builder`)
- [ ] `FlowList.tsx`: Show user flows vs curated flows.
- [ ] `FlowEditor.tsx`: 
  - Add gestures from library.
  - Reorder steps (drag & drop).
  - Edit step overrides (duration, side, notes).
- [ ] Preview Mode: Launch current draft in Player.
